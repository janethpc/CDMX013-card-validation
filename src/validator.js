//const validator = {
  //(property) maskify: (valor: any) => Any
  //maskify: valor=> valor.slice(0, -4).replace(/./g, "#") + valor.slice(-4),

  //funion para comenzar a validar el cc number//
  //isValid: function isValid(){
    //let valid = false
    //let number= [] 
  //}
//};

//export default validator;

export function escribirMensajeConsola(){
  var funcionaBien = document.getElementById("textoPrueba").value; 
  console.log(funcionaBien);
}
